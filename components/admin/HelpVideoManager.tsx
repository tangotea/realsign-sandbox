"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function HelpVideoManager(){const router=useRouter();const [busy,setBusy]=useState(false),[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg("");const fd=new FormData(e.currentTarget);const r=await fetch("/api/admin/help",{method:"POST",body:fd});const j=await r.json();setMsg(r.ok?"Help content saved.":j.error||"Unable to save");setBusy(false);if(r.ok){e.currentTarget.reset();router.refresh()}}return <form className="card" onSubmit={submit}><h2>Add SASL help</h2><label>Title<input className="field" name="title" required/></label><label>Slug<input className="field" name="slug" placeholder="booking-notice" required/></label><label>Text explanation<textarea className="field" name="text" rows={4}/></label><label>SASL video<input className="field" type="file" name="video" accept="video/*"/></label><div className="form-grid"><label>Audience<select className="field" name="audience" defaultValue="everyone"><option value="everyone">Everyone</option><option value="learner">Learner</option><option value="provider">Provider</option><option value="interpreter">Interpreter</option></select></label><label>Screen<select className="field" name="screenKey" defaultValue="general"><option value="general">General Help</option><option value="home">Home</option><option value="booking">Booking</option><option value="provider">Provider</option><option value="video">Video Session</option></select></label><label>Placement key<input className="field" name="placementKey" defaultValue="general"/></label></div><button className="btn" disabled={busy}>{busy?"Saving…":"Save help content"}</button>{msg?<p className="notice">{msg}</p>:null}</form>}
