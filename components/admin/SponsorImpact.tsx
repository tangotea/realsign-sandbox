export default function SponsorImpact({impact,money}:{impact:any;money:(n:number)=>string}){if(!impact)return null;return <div className="tag-row"><span className="pill">{money(impact.used_cents||0)} used</span><span className="pill">{impact.users_supported||0} users supported</span><span className="pill">{impact.bookings_funded||0} bookings</span><span className="pill">{impact.service_hours||0} hours</span></div>}
