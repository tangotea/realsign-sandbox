"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function ReviewModerationActions({reviewId}:{reviewId:string}){const [busy,setBusy]=useState(false),r=useRouter();async function go(action:string){setBusy(true);const res=await fetch('/api/admin/reviews',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({reviewId,action})});const d=await res.json();setBusy(false);if(!res.ok)return alert(d.error);r.refresh()}return <div className="row wrap"><button className="btn" disabled={busy} onClick={()=>go('keep')}>Keep</button><button className="btn secondary" disabled={busy} onClick={()=>go('remove_private_information')}>Remove private info</button><button className="btn ghost" disabled={busy} onClick={()=>go('hide')}>Hide</button></div>}
