"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function RequestActions({id}:{id:string}){const [busy,setBusy]=useState(false);const router=useRouter();async function go(accept:boolean){setBusy(true);const r=await fetch(`/api/interpreter-requests/${id}/respond`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({accept})});const d=await r.json();if(!r.ok){alert(d.error);setBusy(false);return}router.refresh()}return <div className="row wrap"><button className="btn" disabled={busy} onClick={()=>go(true)}>Accept</button><button className="btn ghost" disabled={busy} onClick={()=>go(false)}>Decline</button></div>}
