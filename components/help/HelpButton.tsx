"use client";import {useState} from "react";
type HelpButtonProps={slug:string;label?:string;fallbackText?:string;size?:"mini"|"regular"};
export default function HelpButton({slug,label="Help",fallbackText,size="mini"}:HelpButtonProps){const [open,setOpen]=useState(false),[data,setData]=useState<any>(null),[busy,setBusy]=useState(false);async function show(){setOpen(true);if(data)return;setBusy(true);try{const r=await fetch(`/api/help?slug=${encodeURIComponent(slug)}`);const j=await r.json().catch(()=>({}));setData(r.ok?j:{title:label,text_explanation:fallbackText||j.error||"Help is not available yet."});}catch{setData({title:label,text_explanation:fallbackText||"Help is not available yet."});}finally{setBusy(false)}}return <><button type="button" className={`help-btn${size==="mini"?" mini":""}`} aria-label={label} onClick={show}>?</button>{open?<div className="help-modal-backdrop" onClick={()=>setOpen(false)}><section className="help-modal" role="dialog" aria-modal="true" aria-label={data?.title||label} onClick={e=>e.stopPropagation()}><div className="row"><h2>{data?.title||label}</h2><button className="mini-btn" onClick={()=>setOpen(false)}>Close</button></div>{busy?<p>Loading help…</p>:null}{data?.videoUrl?<video className="help-video" src={data.videoUrl} controls playsInline/>:null}{data?.text_explanation?<p>{data.text_explanation}</p>:null}</section></div>:null}</>}
