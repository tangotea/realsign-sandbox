import Link from "next/link";import {notFound} from "next/navigation";import {createClient} from "@/lib/supabase/server";import MeetRoom from "@/components/meet/MeetRoom";import HelpButton from "@/components/help/HelpButton";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createClient();const {data:a}=await s.auth.getUser();if(!a.user)return <main className="main"><section className="card"><h1>Sign in required</h1><Link className="btn" href="/sign-in">Sign in</Link></section></main>;const {data:ok}=await s.rpc("is_booking_participant",{p_booking_id:id,p_user_id:a.user.id});if(!ok)notFound();return <div className="shell"><header className="topbar"><Link href={`/bookings/${id}`}>←</Link><strong>RealSign Session</strong><HelpButton slug="session-help" label="Session help" size="regular" fallbackText="Use this secure video room for the confirmed RealSign booking. Only the learner and provider can join." /></header><main className="meet-main"><MeetRoom bookingId={id} currentUserId={a.user.id}/></main></div>}
