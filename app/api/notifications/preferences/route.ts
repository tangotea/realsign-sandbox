import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server";
export async function POST(request:Request){const s=await createClient();const {data:a}=await s.auth.getUser();if(!a.user)return NextResponse.json({error:"Sign in required"},{status:401});const body=await request.json();const row={user_id:a.user.id,push_enabled:body.pushEnabled!==false,email_enabled:body.emailEnabled!==false,reminder_24h:body.reminder24h!==false,reminder_1h:body.reminder1h!==false,reminder_10m:body.reminder10m!==false};const {error}=await s.from("notification_preferences").upsert(row,{onConflict:"user_id"});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true})}
