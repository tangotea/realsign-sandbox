import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server";
export async function POST(request:Request){const s=await createClient();const {data:a}=await s.auth.getUser();if(!a.user)return NextResponse.json({error:"Sign in required"},{status:401});try{const {subscription}=await request.json();const endpoint=subscription?.endpoint,p256dh=subscription?.keys?.p256dh,auth=subscription?.keys?.auth;if(!endpoint||!p256dh||!auth)return NextResponse.json({error:"Invalid push subscription"},{status:400});const {error}=await s.from("push_subscriptions").upsert({user_id:a.user.id,endpoint,p256dh,auth_key:auth,user_agent:request.headers.get("user-agent"),active:true},{onConflict:"user_id,endpoint"});if(error)throw error;return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message||"Unable to save push subscription"},{status:500})}}
export async function DELETE(request:Request){const s=await createClient();const {data:a}=await s.auth.getUser();if(!a.user)return NextResponse.json({error:"Sign in required"},{status:401});const {endpoint}=await request.json().catch(()=>({}));if(endpoint)await s.from("push_subscriptions").update({active:false}).eq("user_id",a.user.id).eq("endpoint",endpoint);return NextResponse.json({ok:true})}
