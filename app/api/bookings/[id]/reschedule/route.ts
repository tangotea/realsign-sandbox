import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createClient();const {data:a}=await s.auth.getUser();if(!a.user)return NextResponse.json({error:"Sign in required"},{status:401});const body=await request.json();if(!body.startAt)return NextResponse.json({error:"Choose a new available time"},{status:400});const {data,error}=await s.rpc("request_own_reschedule",{p_booking_id:id,p_new_start_at:body.startAt,p_note:body.note||null});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true,...data})}
