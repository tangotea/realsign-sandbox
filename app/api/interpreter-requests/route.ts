import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";
export async function POST(request:Request){const s=await createClient();const {data:a}=await s.auth.getUser();if(!a.user)return NextResponse.json({error:"Sign in required"},{status:401});try{const b=await request.json();const {data,error}=await s.rpc("create_interpreter_request",{p_provider_id:b.providerId,p_service_id:b.serviceId,p_mode:b.mode||"remote",p_start:b.startAt,p_location:b.location||null,p_context:b.context||"general",p_note:b.note||null});if(error)throw error;return NextResponse.json({ok:true,requestId:data})}catch(e:any){return NextResponse.json({error:e.message||"Unable to create request"},{status:400})}}
