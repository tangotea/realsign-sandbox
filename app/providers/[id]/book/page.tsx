import Link from "next/link"; import { notFound } from "next/navigation"; import AppNav from "@/components/AppNav"; import BookingPicker from "@/components/booking/BookingPicker"; import { createClient } from "@/lib/supabase/server"; import { PublicProvider } from "@/lib/marketplace";
export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{service?:string}>}){const {id}=await params;const q=await searchParams;const supabase=await createClient();const {data}=await supabase.rpc("get_public_provider",{p_provider_id:id});if(!data)notFound();const p=data as PublicProvider;const service=p.services.find(s=>s.id===q.service)||p.services[0];if(!service)notFound();return <div className="shell"><header className="topbar"><Link href={`/providers/${id}`}>←</Link><strong>Choose a time</strong><span /></header><main className="main"><BookingPicker provider={p} service={service}/></main><AppNav/></div>}
